# Core dependencies
typer[all]>=0.9.0
rich>=13.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# API dependencies (for `pr0loader api` command)
# fastapi>=0.109.0
# uvicorn[standard]>=0.27.0
# python-multipart>=0.0.6

# UI dependencies (for `pr0loader ui` command)
# gradio>=4.0.0
# pillow>=10.0.0

# ML dependencies (for training and prediction)
# tensorflow>=2.15.0
# pillow>=10.0.0
